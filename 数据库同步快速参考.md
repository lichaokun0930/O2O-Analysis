# 两台电脑数据库同步 - 快速参考

## 🎯 一句话总结
**所有数据库结构变更用迁移脚本,与代码一起提交Git,B电脑拉取后自动同步**

---

## 📋 A电脑工作流 (修改方)

```powershell
# 1. 修改models.py添加字段
# 2. 创建迁移脚本
cd database\migrations
copy migration_template.sql v2_description.sql
notepad v2_description.sql  # 编辑SQL

# 3. 应用到A电脑数据库
python apply_migration.py v2_description.sql

# 4. 验证
python check_structure.py

# 5. 提交Git
git add database\migrations\v2_description.sql database\models.py
git commit -m "添加XXX字段"
git push
```

---

## 🔄 B电脑工作流 (同步方)

### 方式1: 一键同步(推荐)

```powershell
.\B电脑_同步数据库.ps1
```

### 方式2: 手动执行

```powershell
# 1. 拉取代码
git pull

# 2. 自动同步所有迁移
python database\migrations\sync_migrations.py

# 3. 验证结构
python database\migrations\check_structure.py

# 4. 清理缓存
python 清理Redis缓存.py

# 5. 重启看板
.\启动看板.ps1
```

---

## 🛠️ 常用命令

| 命令 | 用途 |
|------|------|
| `python database\migrations\migration_history.py` | 查看已应用迁移 |
| `python database\migrations\apply_migration.py vX.sql` | 应用单个迁移 |
| `python database\migrations\sync_migrations.py` | 自动同步所有迁移 |
| `python database\migrations\check_structure.py` | 验证结构一致性 |
| `.\B电脑_同步数据库.ps1` | B电脑一键同步 |

---

## 📁 目录结构

```
database/
├── migrations/
│   ├── README.md                    # 迁移目录说明
│   ├── migration_template.sql       # 迁移模板
│   ├── v1_add_stock_fields.sql      # 迁移1: 库存字段
│   ├── migration_history.py         # 管理历史表
│   ├── apply_migration.py           # 应用单个迁移
│   ├── sync_migrations.py           # 自动同步
│   └── check_structure.py           # 验证一致性
```

---

## ✅ 迁移脚本示例

```sql
-- v2_add_new_field.sql
-- 日期: 2025-11-22
-- 说明: 添加新字段

ALTER TABLE orders ADD COLUMN IF NOT EXISTS new_field VARCHAR(100);
COMMENT ON COLUMN orders.new_field IS '字段说明';

-- 验证
DO $$
BEGIN
    IF EXISTS (
        SELECT 1 FROM information_schema.columns
        WHERE table_name = 'orders' AND column_name = 'new_field'
    ) THEN
        RAISE NOTICE '✓ 字段添加成功';
    END IF;
END $$;
```

---

## ⚡ 快速故障排查

### 问题: B电脑报错字段不存在

```powershell
# 1. 检查结构差异
python database\migrations\check_structure.py

# 2. 自动同步
python database\migrations\sync_migrations.py

# 3. 验证
python database\migrations\check_structure.py
```

### 问题: 迁移执行失败

```powershell
# 1. 查看迁移历史
python database\migrations\migration_history.py

# 2. 查看错误信息(在输出中)

# 3. 修复迁移脚本后重新执行
python database\migrations\apply_migration.py vX.sql
```

### 问题: 数据显示异常

```powershell
# 同步后清理缓存
python 清理Redis缓存.py
.\启动看板.ps1
```

---

## 🎓 核心原则

1. **修改models.py → 必须创建迁移脚本**
2. **迁移脚本与代码一起提交Git**
3. **B电脑拉取后自动运行sync_migrations.py**
4. **定期验证check_structure.py**

---

## 📊 完整数据流

```
A电脑修改models.py
    ↓
创建迁移脚本vX.sql
    ↓
应用到A电脑数据库
    ↓
验证结构一致性
    ↓
提交到Git(代码+迁移)
    ↓
B电脑拉取代码
    ↓
自动同步迁移(sync_migrations.py)
    ↓
验证结构一致性
    ↓
清理Redis缓存
    ↓
重启看板
    ↓
✓ 两台电脑完全一致
```

---

## 💡 最佳实践

- ✅ 每次修改立即创建迁移
- ✅ 一个迁移只做一件事
- ✅ 使用有意义的迁移名称
- ✅ 迁移中包含验证逻辑
- ✅ 添加详细的注释说明
- ✅ 提交前先验证A电脑
- ✅ B电脑拉取后立即同步

---

## 🚀 从此告别手动同步!

**A电脑**: 创建迁移 → 提交Git  
**B电脑**: 一键同步 → 搞定!

```powershell
# B电脑只需要一条命令
.\B电脑_同步数据库.ps1
```
