# 客单价分析渠道筛选功能

## 📋 功能概述

为"客单价深度分析"模块添加了多渠道筛选功能,用户可以选择查看特定渠道的客单价分布和影响因素数据,实现与"销售趋势分析"和"一级分类销售趋势"一致的用户体验。

## ✨ 功能特性

### 1. **渠道筛选下拉框**
- 位置: 客单价深度分析标题行
- 选项: 
  - 全部渠道 (默认)
  - 美团闪购
  - 饿了么
  - 京东到家
  - 闪购小程序
- 动态生成: 自动从实际数据中提取可用渠道

### 2. **数据筛选逻辑**
- 先排除咖啡渠道(CHANNELS_TO_REMOVE)
- 再按selected_channel筛选特定渠道
- 空数据友好提示: 显示可用渠道列表

### 3. **闪购小程序数据支持**
- 使用 `calc_mode='all_with_fallback'` 模式
- 包含平台费用为0的订单(闪购小程序的102个订单)
- 确保客单价分布计算准确性

### 4. **影响分析范围**
- 客单价分布(各价格区间订单数、占比)
- 各区间利润率、成本率
- 各区间平均SKU数(购物篮深度)
- 各区间复购率
- 各区间高峰时段

## 🔧 技术实现

### 修改点1: 函数签名 (第2927行)

**修改前:**
```python
def _create_aov_analysis(df: pd.DataFrame, order_agg: pd.DataFrame) -> html.Div:
```

**修改后:**
```python
def _create_aov_analysis(df: pd.DataFrame, order_agg: pd.DataFrame, selected_channel: str = 'all') -> html.Div:
    """
    创建客单价深度分析组件
    
    Args:
        df: 原始订单数据
        order_agg: 订单聚合数据
        selected_channel: 选中的渠道 ('all'表示全部渠道)
    
    Returns:
        客单价分析组件
    """
```

### 修改点2: 渠道筛选逻辑 (第2943行附近)

**修改前:**
```python
# 如果有渠道字段,过滤掉排除的渠道
if '渠道' in order_agg.columns:
    original_count = len(order_agg)
    order_agg = order_agg[~order_agg['渠道'].isin(exclude_channels)].copy()
    filtered_count = original_count - len(order_agg)
    
    if filtered_count > 0:
        print(f"📊 [客单价分析] 已剔除{exclude_channels}渠道订单 {filtered_count} 单，剩余 {len(order_agg)} 单")

if len(order_agg) == 0:
    return html.Div([
        dbc.Alert("剔除特定渠道后无可用数据", color="warning")
    ])
```

**修改后:**
```python
# 🆕 渠道筛选逻辑：支持按渠道筛选或排除咖啡渠道
if '渠道' in order_agg.columns:
    original_count = len(order_agg)
    
    # 先排除咖啡渠道
    order_agg = order_agg[~order_agg['渠道'].isin(exclude_channels)].copy()
    
    # 如果选择了特定渠道,进行筛选
    if selected_channel != 'all':
        order_agg = order_agg[order_agg['渠道'] == selected_channel].copy()
        print(f"📊 [客单价分析-渠道筛选] 渠道='{selected_channel}': {original_count} -> {len(order_agg)} 订单")
    else:
        filtered_count = original_count - len(order_agg)
        if filtered_count > 0:
            print(f"📊 [客单价分析] 已剔除{exclude_channels}渠道订单 {filtered_count} 单，剩余 {len(order_agg)} 单")

if len(order_agg) == 0:
    available_channels = []
    if '渠道' in df.columns:
        available_channels = sorted([ch for ch in df['渠道'].dropna().unique() if ch not in exclude_channels])
    msg = f"⚠️ 渠道 '{selected_channel}' 暂无数据" if selected_channel != 'all' else "剔除特定渠道后无可用数据"
    if available_channels and selected_channel != 'all':
        msg += f"\n\n可用渠道: {', '.join(available_channels)}"
    return html.Div([
        dbc.Alert(msg, color="warning", style={'whiteSpace': 'pre-wrap'})
    ])
```

### 修改点3: UI下拉框 (第3430行附近)

**修改前:**
```python
dbc.Row([
    dbc.Col([
        html.H4("💰 客单价深度分析", className="mb-0")
    ], width=4),
    dbc.Col([
        dbc.ButtonGroup([...])
    ], width=8)
])
```

**修改后:**
```python
dbc.Row([
    dbc.Col([
        html.H4("💰 客单价深度分析", className="mb-0")
    ], width=3),
    dbc.Col([
        # 🆕 渠道筛选下拉菜单
        dcc.Dropdown(
            id='aov-analysis-channel-filter',
            options=[],  # 将由callback动态设置
            value='all',
            placeholder='选择渠道',
            clearable=False,
            className="dash-bootstrap"
        )
    ], width=3),
    dbc.Col([
        dbc.ButtonGroup([...])
    ], width=6)
])
```

### 修改点4: 异步加载修改 (第10457行附近)

**修改前:**
```python
# 渲染客单价分析
aov_analysis = _create_aov_analysis(df, order_agg)

print(f"✅ [异步加载] Tab1客单价分析渲染完成", flush=True)
return aov_analysis
```

**修改后:**
```python
# 渲染客单价分析 (初始为全部渠道)
aov_analysis = _create_aov_analysis(df, order_agg, selected_channel='all')

# 🆕 获取渠道列表用于下拉菜单
channel_options = [{'label': '全部渠道', 'value': 'all'}]
if '渠道' in df.columns:
    unique_channels = sorted([ch for ch in df['渠道'].dropna().unique() if ch])
    channel_options.extend([{'label': ch, 'value': ch} for ch in unique_channels])

# 🆕 将内容包装在容器中,并添加Store存储channel_options
result = html.Div([
    dcc.Store(id='aov-channel-options-store', data=channel_options),
    html.Div(id='aov-analysis-content-container', children=[aov_analysis])
])

print(f"✅ [异步加载] Tab1客单价分析渲染完成", flush=True)
return result
```

### 修改点5: Callback - 选项初始化 (新增)

```python
@app.callback(
    Output('aov-analysis-channel-filter', 'options'),
    Input('aov-channel-options-store', 'data'),
    prevent_initial_call=False
)
def update_aov_channel_options(channel_options):
    """从Store初始化客单价分析的渠道下拉框选项"""
    if channel_options:
        return channel_options
    return [{'label': '全部渠道', 'value': 'all'}]
```

### 修改点6: Callback - 渠道筛选 (新增)

```python
@app.callback(
    Output('aov-analysis-content-container', 'children'),
    Input('aov-analysis-channel-filter', 'value'),
    [State('cached-order-agg', 'data'),
     State('cache-version', 'data')],
    prevent_initial_call=True
)
def update_aov_analysis_by_channel(selected_channel, cached_agg, cache_version):
    """根据渠道筛选更新客单价分析"""
    try:
        if GLOBAL_DATA is None or GLOBAL_DATA.empty:
            return dbc.Alert("⚠️ 数据不可用", color="warning")
        
        df = GLOBAL_DATA.copy()
        
        # 从缓存或重新计算订单聚合数据
        if cached_agg is not None:
            order_agg = pd.DataFrame(cached_agg)
            # 检查缓存是否包含必要字段
            if '配送净成本' not in order_agg.columns or '订单总收入' not in order_agg.columns:
                print(f"⚠️ [客单价-渠道筛选] 缓存数据缺少关键字段,重新计算...")
                order_agg = calculate_order_metrics(df, calc_mode='all_with_fallback')
        else:
            order_agg = calculate_order_metrics(df, calc_mode='all_with_fallback')
        
        # 🔍 调试日志
        print(f"\n🔍 [客单价-渠道筛选] 渠道='{selected_channel}', 订单数={len(order_agg)}")
        if '渠道' in order_agg.columns:
            print(f"   渠道分布: {order_agg['渠道'].value_counts().to_dict()}")
        
        # 调用修改后的分析函数,传入selected_channel参数
        return _create_aov_analysis(df, order_agg, selected_channel)
    
    except Exception as e:
        print(f"❌ [update_aov_analysis_by_channel] 错误: {e}")
        import traceback
        traceback.print_exc()
        return dbc.Alert(f"客单价分析更新失败: {str(e)}", color="danger")
```

## 📊 数据流程

```
用户选择渠道
    ↓
Callback触发: update_aov_analysis_by_channel(selected_channel)
    ↓
加载全局数据: GLOBAL_DATA
    ↓
订单聚合: calculate_order_metrics(df, calc_mode='all_with_fallback')
    ↓
调用分析函数: _create_aov_analysis(df, order_agg, selected_channel)
    ↓
合并渠道信息: order_agg + channel map
    ↓
排除咖啡渠道: order_agg[~isin(CHANNELS_TO_REMOVE)]
    ↓
渠道筛选: order_agg[渠道 == selected_channel] (如果selected_channel != 'all')
    ↓
计算客单价分布: pd.cut(客单价, bins) → 8个价格区间
    ↓
计算各区间指标: 订单数、占比、利润率、成本率、亏损订单占比、平均利润、销售额占比、SKU数等
    ↓
生成图表和卡片
    ↓
更新容器: aov-analysis-content-container
```

## 🎯 使用场景

### 场景1: 查看全部渠道客单价分布
1. 选择"全部渠道"(默认)
2. 显示所有渠道的客单价分布
3. 包含所有5,900个订单(排除咖啡渠道)

### 场景2: 分析特定渠道客单价结构
1. 选择"美团闪购"
2. 只显示美团闪购3,479个订单的客单价分布
3. 查看该渠道各价格区间的订单占比和盈利能力

### 场景3: 对比不同渠道客单价特征
1. 先选择"美团闪购",查看其主流区在¥30-40
2. 再选择"饿了么",对比其主流区是否相同
3. 分析不同渠道的客单价结构差异

### 场景4: 分析闪购小程序客单价
1. 选择"闪购小程序"
2. 查看102个订单的价格分布
3. 验证低价区订单占比和盈利能力
4. 确认数据正确包含(之前被过滤)

## 📈 各渠道客单价特征对比

| 渠道 | 订单数 | 平均客单价 | 主流价格区间 | 低价区占比 | 高价区占比 |
|------|--------|------------|--------------|------------|------------|
| 全部渠道 | 5,900 | ~35元 | ¥20-50 | ~30% | ~15% |
| 美团闪购 | 3,479 | ~36元 | ¥30-50 | ~28% | ~17% |
| 饿了么 | 2,037 | ~34元 | ¥20-40 | ~32% | ~13% |
| 京东到家 | 282 | ~38元 | ¥30-50 | ~25% | ~18% |
| 闪购小程序 | 102 | ~28元 | ¥10-30 | ~45% | ~8% |

*注: 以上数据为示例,实际数据以系统显示为准*

## ✅ 测试验证

### 自动验证脚本
运行 `验证客单价渠道筛选.py` 验证所有修改点:

```bash
python 验证客单价渠道筛选.py
```

验证项:
- ✅ 函数签名参数 (selected_channel='all')
- ✅ UI下拉框 (aov-analysis-channel-filter)
- ✅ 渠道筛选逻辑修改
- ✅ 内容容器 (aov-analysis-content-container)
- ✅ 选项存储 (aov-channel-options-store)
- ✅ 选项初始化callback
- ✅ 渠道筛选callback
- ✅ 异步加载修改
- ✅ 使用all_with_fallback模式

### 手动测试步骤

1. **启动系统**
   ```bash
   python 智能门店看板_Dash版.py
   ```

2. **访问看板**
   - 浏览器打开: http://localhost:8050
   - 上传测试数据

3. **测试渠道筛选**
   - 在Tab1找到"💰 客单价深度分析"模块
   - 确认标题行有渠道下拉框
   - 依次选择每个渠道:
     * 全部渠道 → 显示所有渠道数据
     * 美团闪购 → 显示美团数据
     * 饿了么 → 显示饿了么数据
     * 京东到家 → 显示京东数据
     * 闪购小程序 → 显示闪购小程序数据

4. **验证数据准确性**
   - 检查各价格区间订单数之和 = 总订单数
   - 验证净利率计算: 利润额 / 订单总收入
   - 验证销售额占比总和 = 100%
   - 检查购物篮深度(平均SKU数)是否合理

5. **验证闪购小程序**
   - 选择"闪购小程序"
   - 确认有102个订单
   - 检查价格分布(预期低价区占比较高)
   - 验证利润率和成本率

## 🔄 与其他模块对比

| 特性 | 销售趋势分析 | 一级分类趋势 | 客单价分析 |
|------|-------------|-------------|-----------|
| 渠道筛选 | ✅ 支持 | ✅ 支持 | ✅ 支持 |
| 下拉框位置 | 标题行右侧 | 标题行中间 | 标题行中间 |
| 数据维度 | 日期 | 分类 | 价格区间 |
| 计算模式 | all_with_fallback | all_with_fallback | all_with_fallback |
| 闪购小程序 | ✅ 包含 | ✅ 包含 | ✅ 包含 |
| 空数据提示 | ✅ 友好提示 | ✅ 友好提示 | ✅ 友好提示 |
| 异步加载 | ❌ | ❌ | ✅ |

## 💡 设计理念

1. **一致性**: 与销售趋势分析和分类趋势保持相同的交互模式
2. **完整性**: 使用all_with_fallback模式,确保所有订单都被统计
3. **友好性**: 空数据时提供可用渠道列表
4. **性能**: 利用异步加载机制,使用缓存优化性能
5. **可维护性**: 复用channel_options生成逻辑,减少重复代码

## 🐛 已知问题

无

## 📝 后续优化建议

1. **缓存优化**: 按渠道缓存客单价分析结果
2. **导出功能**: 导出时包含渠道信息
3. **对比分析**: 支持多渠道对比(如:美团 vs 饿了么客单价对比)
4. **趋势分析**: 按渠道显示客单价随时间变化趋势
5. **智能推荐**: 根据渠道特征推荐最优定价策略

## 📚 相关文件

- `智能门店看板_Dash版.py`: 主文件,包含所有修改
- `验证客单价渠道筛选.py`: 自动验证脚本
- `分类趋势渠道筛选功能说明.md`: 分类趋势筛选说明
- `利润计算逻辑说明.md`: 利润计算模式说明

## 👥 影响范围

- **用户界面**: Tab1 客单价深度分析模块
- **数据处理**: _create_aov_analysis函数
- **回调函数**: 新增2个callback (选项初始化 + 渠道筛选)
- **异步加载**: async_load_tab1_aov_section修改
- **数据完整性**: 确保闪购小程序数据被正确统计

## ✨ 总结

通过6个关键修改点,成功为客单价深度分析模块添加了渠道筛选功能:
1. 函数参数 (支持渠道传递)
2. 渠道筛选逻辑 (数据过滤)
3. UI下拉框 (用户交互)
4. 异步加载修改 (生成options和容器)
5. 选项初始化callback (动态填充)
6. 渠道筛选callback (响应交互)

功能实现与销售趋势和分类趋势分析保持一致,用户体验良好,数据准确完整。特别地,闪购小程序的102个订单现在能够正确显示在各个价格区间中。
