# 智能门店经营看板系统 - 依赖安装指南

# =============================================================================
# 重要说明：本系统不需要下载大型语言模型(LLM)！
# 我们构建的是基于算法和统计模型的"智能分析引擎"，不是ChatGPT那种大模型
# =============================================================================

cd D:\Python1\O2O_Analysis\O2O数据分析
.\.venv\Scripts\Activate.ps1

Write-Host "🚀 开始安装智能门店经营看板系统依赖..." -ForegroundColor Green
Write-Host ""

# =============================================================================
# 1. 核心数据处理库
# =============================================================================

Write-Host "📊 安装数据处理库..." -ForegroundColor Yellow
pip install pandas==2.0.3
pip install numpy==1.24.3
pip install openpyxl==3.1.2        # Excel文件支持
pip install xlrd==2.0.1            # 老版本Excel支持

# =============================================================================  
# 2. 可视化和界面库
# =============================================================================

Write-Host "🎨 安装可视化库..." -ForegroundColor Yellow
pip install streamlit==1.28.1      # Web界面框架
pip install plotly==5.17.0         # 交互式图表
pip install matplotlib==3.7.2      # 基础绘图
pip install seaborn==0.12.2        # 统计图表

# =============================================================================
# 3. 机器学习和统计库 (轻量级)
# =============================================================================

Write-Host "🤖 安装分析算法库..." -ForegroundColor Yellow
pip install scikit-learn==1.3.0    # 机器学习算法
pip install scipy==1.11.1          # 科学计算
pip install statsmodels==0.14.0    # 统计模型

# =============================================================================
# 4. 辅助工具库
# =============================================================================

Write-Host "🔧 安装辅助工具..." -ForegroundColor Yellow
pip install tqdm==4.65.0           # 进度条显示
pip install python-dateutil==2.8.2 # 日期处理
pip install pytz==2023.3           # 时区处理

# =============================================================================
# 5. 可选：性能优化库
# =============================================================================

Write-Host "⚡ 安装性能优化库 (可选)..." -ForegroundColor Cyan
pip install numba==0.57.1          # 数值计算加速
pip install joblib==1.3.1          # 并行处理

Write-Host ""
Write-Host "✅ 所有依赖安装完成！" -ForegroundColor Green
Write-Host "💡 总安装大小约: 500MB" -ForegroundColor Cyan
Write-Host "⏱️  预计安装时间: 5-10分钟" -ForegroundColor Cyan
Write-Host ""

# =============================================================================
# 6. 验证安装
# =============================================================================

Write-Host "🧪 验证关键库安装..." -ForegroundColor Yellow

python -c "import pandas; print(f'✅ pandas {pandas.__version__}')"
python -c "import numpy; print(f'✅ numpy {numpy.__version__}')"
python -c "import streamlit; print(f'✅ streamlit {streamlit.__version__}')"
python -c "import plotly; print(f'✅ plotly {plotly.__version__}')"
python -c "import sklearn; print(f'✅ scikit-learn {sklearn.__version__}')"

Write-Host ""
Write-Host "🎯 现在可以启动智能看板了！" -ForegroundColor Green
Write-Host "   运行命令: streamlit run 测算模型\智能门店经营看板_可视化.py" -ForegroundColor Cyan