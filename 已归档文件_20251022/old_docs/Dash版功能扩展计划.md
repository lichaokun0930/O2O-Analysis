# 📋 Dash版功能扩展计划

## 🎯 目标

将Streamlit版本的**所有6个诊断Tab**完整集成到Dash版本。

---

## 📊 当前状态

### ✅ 已实现（Dash v1.0）

| 模块 | 功能 | 完成度 |
|------|------|--------|
| **数据加载** | 多路径扫描、示例数据兜底 | 100% ✅ |
| **筛选器** | 场景、时段、排序 | 100% ✅ |
| **统计卡片** | 4个核心指标 | 100% ✅ |
| **数据表格** | 原生排序、筛选、分页 | 100% ✅ |
| **Excel导出** | 多Sheet导出 | 100% ✅ |
| **基础诊断** | 销量下滑商品列表 | 30% ⚠️ |

### ❌ 缺失功能（需要集成）

| Tab序号 | Tab名称 | 核心功能 | Streamlit行数 | 优先级 |
|---------|---------|----------|--------------|--------|
| **Tab 1** | 📉 销量下滑 | ✅ 基础列表已实现<br>❌ 可视化图表缺失<br>❌ 周期对比缺失<br>❌ 分时段分析缺失 | ~450行 | ⭐⭐⭐⭐⭐ |
| **Tab 2** | 💰 客单价归因 | ❌ 完全缺失 | ~300行 | ⭐⭐⭐⭐ |
| **Tab 3** | 🚨 负毛利预警 | ❌ 完全缺失 | ~250行 | ⭐⭐⭐⭐ |
| **Tab 4** | 🚚 高配送费 | ❌ 完全缺失 | ~200行 | ⭐⭐⭐ |
| **Tab 5** | ⚖️ 角色失衡 | ❌ 完全缺失 | ~180行 | ⭐⭐⭐ |
| **Tab 6** | 📊 异常波动 | ❌ 完全缺失 | ~150行 | ⭐⭐⭐ |

---

## 🚀 实施计划

### 阶段1：完善Tab 1 - 销量下滑（优先级最高）⭐⭐⭐⭐⭐

**目标**：将Streamlit版本的所有可视化和分析功能集成到Dash版本

#### 1.1 核心指标卡片 ✅（已有基础）
```python
# 现状：已有4个统计卡片
# 优化：添加更多指标
- 下滑商品数 ✅
- 总销量损失 ✅
- 总收入损失 ✅
- 总利润损失 ✅
- 平均下滑幅度（新增）
- 最严重商品（新增）
```

#### 1.2 可视化图表（需新增）❌

**图表1：分时段下滑分布**
```python
dcc.Graph(id='chart-slot-distribution')

@app.callback(
    Output('chart-slot-distribution', 'figure'),
    Input('current-data-store', 'data')
)
def update_slot_chart(data):
    # 按时段分组，计算下滑商品数和损失金额
    # 使用plotly.graph_objects.Bar绘制
    return fig
```

**图表2：周期对比图**
```python
dcc.Graph(id='chart-period-comparison')

@app.callback(
    Output('chart-period-comparison', 'figure'),
    Input('current-data-store', 'data')
)
def update_period_chart(data):
    # 双柱状图：对比周期销量 vs 当前周期销量
    return fig
```

**图表3：分类损失排名**
```python
dcc.Graph(id='chart-category-loss')

@app.callback(
    Output('chart-category-loss', 'figure'),
    Input('current-data-store', 'data')
)
def update_category_chart(data):
    # 横向条形图：收入损失最大的5个品类
    return fig
```

**图表4：各分类下滑TOP商品**
```python
dcc.Graph(id='chart-category-top-products')

@app.callback(
    Output('chart-category-top-products', 'figure'),
    Input('current-data-store', 'data')
)
def update_category_top_chart(data):
    # 每个分类取下滑最严重的前3个商品
    return fig
```

**图表5：四维散点图**
```python
dcc.Graph(id='chart-scatter-4d')

@app.callback(
    Output('chart-scatter-4d', 'figure'),
    Input('current-data-store', 'data')
)
def update_scatter_chart(data):
    # X轴：销量变化
    # Y轴：利润损失
    # 气泡大小：商品售价
    # 颜色：毛利率
    return fig
```

**图表6：下滑商品价格分布**
```python
dcc.Graph(id='chart-price-distribution')

@app.callback(
    Output('chart-price-distribution', 'figure'),
    Input('current-data-store', 'data')
)
def update_price_chart(data):
    # 箱线图或直方图：价格分布
    return fig
```

#### 1.3 交互式商品明细（需增强）⚠️

**当前状态**：
- ✅ 基础表格显示
- ❌ 缺少行点击查看详情
- ❌ 缺少商品卡片展示

**目标功能**：
```python
# 添加Modal弹窗查看商品详情
dbc.Modal([
    dbc.ModalHeader("商品详细信息"),
    dbc.ModalBody(id='product-detail-body'),
    dbc.ModalFooter(dbc.Button("关闭", id='close-modal'))
], id='product-modal', size='lg')

@app.callback(
    [Output('product-modal', 'is_open'),
     Output('product-detail-body', 'children')],
    [Input('detail-table', 'active_cell'),
     Input('close-modal', 'n_clicks')],
    State('current-data-store', 'data')
)
def show_product_detail(active_cell, n_close, data):
    # 点击表格行时显示商品详细信息
    # 包括：历史趋势图、场景分析、时段分析
    return is_open, detail_content
```

#### 1.4 周期选择器（需新增）❌

```python
# 添加自定义周期对比功能
dbc.Card([
    dbc.CardHeader("📅 自定义周期对比"),
    dbc.CardBody([
        dbc.Row([
            dbc.Col([
                html.Label("对比周期"),
                dcc.Dropdown(id='compare-period-selector')
            ], md=6),
            dbc.Col([
                html.Label("当前周期"),
                dcc.Dropdown(id='current-period-selector')
            ], md=6)
        ])
    ])
])

@app.callback(
    [Output('compare-period-selector', 'options'),
     Output('current-period-selector', 'options')],
    Input('apply-filter-btn', 'n_clicks')
)
def init_period_options(n):
    # 从诊断引擎获取可用周期列表
    periods = DIAGNOSTIC_ENGINE.get_available_periods()
    return periods, periods
```

---

### 阶段2：新增Tab 2 - 客单价归因 ⭐⭐⭐⭐

**核心功能**：
1. **客单价变化趋势图**
   - 时间序列折线图
   - 对比上周期客单价

2. **归因分析**
   - 商品数量变化
   - 商品单价变化
   - 组合变化（数量×单价）

3. **商品结构分析**
   - 高价商品占比变化
   - 低价商品占比变化

**实现代码**：
```python
# 在layout中添加新Tab
dbc.Tabs([
    dbc.Tab(label="📉 销量下滑", tab_id="tab-decline"),
    dbc.Tab(label="💰 客单价归因", tab_id="tab-aov"),  # 新增
    # ...其他Tab
], id="diagnostic-tabs", active_tab="tab-decline")

# 添加Tab内容容器
html.Div(id='tab-content')

# 回调函数切换Tab内容
@app.callback(
    Output('tab-content', 'children'),
    Input('diagnostic-tabs', 'active_tab')
)
def render_tab_content(active_tab):
    if active_tab == 'tab-decline':
        return decline_layout
    elif active_tab == 'tab-aov':
        return aov_layout  # 客单价归因布局
    # ...
```

**客单价归因布局**：
```python
aov_layout = dbc.Container([
    # 统计卡片
    dbc.Row([
        dbc.Col([dbc.Card([...])], md=3),  # 当前客单价
        dbc.Col([dbc.Card([...])], md=3),  # 对比周期客单价
        dbc.Col([dbc.Card([...])], md=3),  # 客单价变化
        dbc.Col([dbc.Card([...])], md=3),  # 变化幅度
    ]),
    
    # 图表区域
    dbc.Row([
        dbc.Col([dcc.Graph(id='aov-trend-chart')], md=6),
        dbc.Col([dcc.Graph(id='aov-attribution-chart')], md=6)
    ]),
    
    # 明细表格
    dbc.Card([
        dash_table.DataTable(id='aov-detail-table')
    ])
])
```

---

### 阶段3：新增Tab 3 - 负毛利预警 ⭐⭐⭐⭐

**核心功能**：
1. **负毛利商品列表**
   - 毛利率 < 0% 的商品
   - 按损失金额排序

2. **预警等级**
   - 🔴 严重：毛利率 < -20%
   - 🟠 警告：毛利率 -20% ~ 0%
   - 🟡 关注：毛利率 0% ~ 10%

3. **原因分析**
   - 成本高于售价
   - 折扣力度过大
   - 配送费倒挂

**实现代码**：
```python
negative_margin_layout = dbc.Container([
    # 预警级别统计
    dbc.Row([
        dbc.Col([dbc.Card([...], style={'background': '#ffebee'})], md=4),  # 严重
        dbc.Col([dbc.Card([...], style={'background': '#fff3e0'})], md=4),  # 警告
        dbc.Col([dbc.Card([...], style={'background': '#fff9c4'})], md=4),  # 关注
    ]),
    
    # 图表：毛利率分布
    dcc.Graph(id='margin-distribution-chart'),
    
    # 明细表格（带颜色标记）
    dash_table.DataTable(
        id='negative-margin-table',
        style_data_conditional=[
            {'if': {'filter_query': '{毛利率%} < -20'}, 
             'backgroundColor': '#ffebee'},
            {'if': {'filter_query': '{毛利率%} >= -20 && {毛利率%} < 0'}, 
             'backgroundColor': '#fff3e0'},
        ]
    )
])
```

---

### 阶段4：新增Tab 4/5/6 - 其他诊断 ⭐⭐⭐

**Tab 4 - 高配送费**：
- 配送费占订单金额比例 > 阈值
- 按配送费占比排序
- 优化建议（满减策略）

**Tab 5 - 角色失衡**：
- 配角商品销量 > 主角商品
- 结构不合理提示

**Tab 6 - 异常波动**：
- 3σ原则检测异常
- 突增/突降预警

---

## 📐 布局设计

### 完整布局结构

```python
app.layout = dbc.Container([
    # 1. 头部
    html.Div([...], className='main-header'),
    
    # 2. 筛选器卡片（全局）
    dbc.Card([...]),
    
    # 3. Tab导航
    dbc.Tabs([
        dbc.Tab(label="📉 销量下滑", tab_id="tab-decline"),
        dbc.Tab(label="💰 客单价归因", tab_id="tab-aov"),
        dbc.Tab(label="🚨 负毛利预警", tab_id="tab-margin"),
        dbc.Tab(label="🚚 高配送费", tab_id="tab-delivery"),
        dbc.Tab(label="⚖️ 角色失衡", tab_id="tab-role"),
        dbc.Tab(label="📊 异常波动", tab_id="tab-anomaly"),
    ], id="diagnostic-tabs", active_tab="tab-decline"),
    
    # 4. Tab内容容器
    html.Div(id='tab-content'),
    
    # 5. 数据存储
    dcc.Store(id='current-data-store'),
    dcc.Download(id='download-excel')
], fluid=True)
```

---

## ⏱️ 实施时间表

| 阶段 | 任务 | 预计时间 | 状态 |
|------|------|---------|------|
| **阶段1.1** | 完善Tab1核心指标 | 30分钟 | ⏳ 待开始 |
| **阶段1.2** | 添加6个可视化图表 | 2小时 | ⏳ 待开始 |
| **阶段1.3** | 增强交互式明细 | 1小时 | ⏳ 待开始 |
| **阶段1.4** | 新增周期选择器 | 1小时 | ⏳ 待开始 |
| **阶段2** | 新增客单价归因Tab | 1.5小时 | ⏳ 待开始 |
| **阶段3** | 新增负毛利预警Tab | 1小时 | ⏳ 待开始 |
| **阶段4** | 新增其他3个Tab | 2小时 | ⏳ 待开始 |
| **测试** | 完整功能测试 | 1小时 | ⏳ 待开始 |

**总计**：约10小时

---

## 🎯 验收标准

### 功能完整性

| 功能类别 | Streamlit版 | Dash版目标 | 当前状态 |
|---------|-------------|-----------|---------|
| **Tab数量** | 6个 | 6个 | 1个 ❌ |
| **图表数量** | 20+ | 20+ | 0个 ❌ |
| **筛选器** | 5个 | 5个 | 3个 ⚠️ |
| **统计指标** | 15+ | 15+ | 4个 ⚠️ |
| **导出功能** | Excel多Sheet | Excel多Sheet | ✅ |
| **交互体验** | ❌ 页面跳转 | ✅ 无跳转 | ✅ |

### 性能要求

- 筛选响应时间 < 0.5秒 ✅
- 图表渲染时间 < 1秒
- Tab切换时间 < 0.3秒
- Excel导出时间 < 5秒 ✅

### 用户体验

- ✅ 页面完全无跳转
- ✅ 所有图表交互流畅
- ✅ 数据加载有进度提示
- ✅ 错误处理友好

---

## 💡 实施建议

### 方案A：渐进式扩展（推荐）⭐⭐⭐⭐⭐

**优点**：
- 每个阶段可独立测试
- 用户可立即体验新功能
- 降低开发风险

**缺点**：
- 总时间较长（分多次会话）

**适用场景**：稳健迭代，确保质量

---

### 方案B：一次性完成

**优点**：
- 一次交付完整功能
- 避免多次代码合并

**缺点**：
- 开发时间长（10小时）
- 调试复杂度高
- 可能超出token限制

**适用场景**：有充足时间和资源

---

## 🚀 立即开始？

**建议从阶段1开始**：完善Tab 1的可视化功能

**理由**：
1. 销量下滑是最核心的诊断功能
2. 图表库(Plotly)已经引入，可复用
3. 数据已经准备好，只需添加可视化
4. 用户可立即看到显著改进

**预计完成时间**：4-5小时

---

**是否开始实施？请选择：**
- A. 开始阶段1.1-1.4（完善销量下滑Tab）⭐ 推荐
- B. 开始阶段2（新增客单价归因Tab）
- C. 一次性完成所有6个Tab
- D. 先查看详细代码示例再决定
