# 修复智能门店经营看板_可视化.py的语法错误

# 读取文件内容
with open('智能门店经营看板_可视化.py', 'r', encoding='utf-8') as f:
    lines = f.readlines()

# 修复第1130行的缩进 (0-indexed: 1129)
if len(lines) > 1129:  
    lines[1129] = '                else:\n'
    print('修复了第1130行的缩进')

# 修复第1131行的缩进 (0-indexed: 1130) 
if len(lines) > 1130:  
    lines[1130] = '                    st.warning("🚫 数据中未包含有效指标")\n'
    print('修复了第1131行的缩进')

# 写回文件
with open('智能门店经营看板_可视化.py', 'w', encoding='utf-8') as f:
    f.writelines(lines)

print('文件已更新，语法错误已修复')