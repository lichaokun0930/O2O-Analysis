# -*- coding: utf-8 -*-
"""修复组合类型映射"""

# 读取文件
with open('多商品订单引导分析看板.py', 'r', encoding='utf-8') as f:
    content = f.read()

# 替换旧的类型映射
old_mapping = """                        type_emoji = {
                            '引流组合': '🎁',
                            '低价组合': '🍭',
                            '中价组合': '🛒',
                            '高价组合': '💎'
                        }.get(row['组合类型'], '📦')"""

new_mapping = """                        type_emoji = {
                            '超低价引流组合': '🎁',
                            '低价快消组合': '🍬',
                            '中低价日用组合': '🛍️',
                            '中价标品组合': '🛒',
                            '高价囤货组合': '💎'
                        }.get(row['组合类型'], '📦')"""

content = content.replace(old_mapping, new_mapping)

# 保存文件
with open('多商品订单引导分析看板.py', 'w', encoding='utf-8') as f:
    f.write(content)

print("✅ 文件已修复！")
